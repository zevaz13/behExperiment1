#pragma once

// Constants shared by both experiments. Per-experiment defaults live in
// behavioralConfig.h / gridConfig.h.
constexpr unsigned long kSerialBaud      = 38400;
constexpr int kPwmResolutionBits         = 12;
