#ifndef SOLID_MODE_H
#define SOLID_MODE_H

// Run Solid mode: applies current ledVal[], monitors button presses,
// sets pressFlag + increments trCnt on each press. Returns when started=false.
void runSolid();

#endif
